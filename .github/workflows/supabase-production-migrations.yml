name: Canonical Supabase Production Migrations

on:
  workflow_dispatch:
    inputs:
      mode:
        description: DRY_RUN is default and never mutates production; APPLY requires exact confirmation.
        required: true
        default: DRY_RUN
        type: choice
        options: [DRY_RUN, APPLY]
      expected_git_sha:
        description: Exact main commit SHA to deploy.
        required: true
        type: string
      expected_versions:
        description: Comma-separated exact 14-digit pending versions, or NONE for DRY_RUN only.
        required: true
        type: string
      apply_confirmation:
        description: For APPLY only, enter exactly APPLY <expected_versions>.
        required: false
        default: ''
        type: string

concurrency:
  group: supabase-production-migrations
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  canonical-migration-deploy:
    runs-on: ubuntu-24.04
    environment: production
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_PRODUCTION_DB_PASSWORD }}
      SUPABASE_PROJECT_REF: odlrhijtfyavryeqivaa
      SUPABASE_CLI_VERSION: 2.79.0
    steps:
      # actions/checkout v4.2.2
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0

      # supabase/setup-cli v1.5.0
      - uses: supabase/setup-cli@d347ba47d3fb7eeeddbbc793bc8d4779caf773ea
        with:
          version: ${{ env.SUPABASE_CLI_VERSION }}

      - name: Verify dispatch and immutable target
        shell: bash
        env:
          EXPECTED_GIT_SHA: ${{ inputs.expected_git_sha }}
        run: |
          set -euo pipefail
          expected_git_sha="${EXPECTED_GIT_SHA}"
          test "${GITHUB_REF}" = "refs/heads/main"
          if ! [[ "${expected_git_sha}" =~ ^[0-9a-f]{40}$ ]]; then
            echo "CANONICAL_MIGRATION_GUARD: expected_git_sha must be a full lowercase 40-hex SHA" >&2
            exit 1
          fi
          checked_out_sha="$(git rev-parse HEAD)"
          current_main_sha="$(git ls-remote origin refs/heads/main | awk '{print $1}')"
          if ! [[ "${current_main_sha}" =~ ^[0-9a-f]{40}$ ]]; then
            echo "CANONICAL_MIGRATION_GUARD: could not resolve current remote main HEAD" >&2
            exit 1
          fi
          if [ "${checked_out_sha}" != "${expected_git_sha}" ] || [ "${current_main_sha}" != "${expected_git_sha}" ]; then
            echo "EXPECTED MAIN SHA: ${expected_git_sha}" >&2
            echo "CURRENT REMOTE MAIN SHA: ${current_main_sha}" >&2
            echo "CHECKED OUT HEAD: ${checked_out_sha}" >&2
            echo "CANONICAL_MIGRATION_GUARD: expected_git_sha is not current main HEAD" >&2
            exit 1
          fi
          test -n "${SUPABASE_ACCESS_TOKEN}"
          test -n "${SUPABASE_DB_PASSWORD}"
          test "${SUPABASE_PROJECT_REF}" = "odlrhijtfyavryeqivaa"
          if [ "${{ inputs.mode }}" = "APPLY" ] && [ "${{ inputs.expected_versions }}" = "NONE" ]; then
            echo "NONE is valid only for DRY_RUN" >&2
            exit 1
          fi
          if [ "${{ inputs.mode }}" = "APPLY" ]; then
            test "${{ inputs.apply_confirmation }}" = "APPLY ${{ inputs.expected_versions }}"
          fi

      - name: Validate canonical migration filenames
        shell: bash
        run: |
          set -euo pipefail
          find supabase/migrations -maxdepth 1 -type f -name '*.sql' -printf '%f\n' | sort | \
            awk 'BEGIN { ok=1 } !/^[0-9]{14}_[A-Za-z0-9][A-Za-z0-9_-]*\.sql$/ { print "invalid migration filename: " $0 > "/dev/stderr"; ok=0 } END { exit ok ? 0 : 1 }'

      - name: Link only the production project
        shell: bash
        run: |
          set -euo pipefail
          supabase link --project-ref "${SUPABASE_PROJECT_REF}" --password "${SUPABASE_DB_PASSWORD}"

      - name: Read canonical migration status
        shell: bash
        run: |
          set -euo pipefail
          supabase migration list --linked | tee migration-list.before.txt
          node scripts/supabase/verify-canonical-migration-ledger.mjs supabase/migrations "${{ inputs.expected_versions }}" migration-list.before.txt
          supabase db push --dry-run --linked | tee db-push-dry-run.txt
          if [ "${{ inputs.expected_versions }}" = "NONE" ] && ! grep -Eqi 'no.*migration|nothing to apply|up to date' db-push-dry-run.txt; then
            echo "DRY_RUN NONE did not prove zero migrations to apply" >&2
            exit 1
          fi

      - name: Apply exactly the preflighted set
        if: inputs.mode == 'APPLY'
        shell: bash
        run: |
          set -euo pipefail
          supabase db push --linked

      - name: Verify final canonical ledger
        if: inputs.mode == 'APPLY'
        shell: bash
        run: |
          set -euo pipefail
          supabase migration list --linked | tee migration-list.after.txt
          node scripts/supabase/verify-canonical-migration-ledger.mjs supabase/migrations "${{ inputs.expected_versions }}" migration-list.after.txt after-apply
